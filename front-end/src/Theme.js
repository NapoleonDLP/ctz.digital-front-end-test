import { createTheme } from '@mui/material/styles';

let theme = createTheme({
  palette: {
    primary: {
      main: '#1E9AAA'
    },
    secondary: {
      main: '#fdb000'
    },
    tertiary: {
      main: '#06d6a0'
    },
    black: {
      main: '#4e4e4e'
    },
    white: {
      main: '#ffffff'
    },
  }
});

export default theme;
